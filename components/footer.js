import styles from "../styles/Footer.module.scss";

export default function Footer() {
  return (
    <footer className={`${styles.footer} text-center text-muted`}>
      <p>&copy; MMXXII Eleanor L. M.</p>
    </footer>
  );
}
